<Macro feature-common-method-filter>
    # Disable all methods except POST, GET and HEAD
    RewriteCond %{REQUEST_METHOD} !^(POST|GET|HEAD)
    RewriteRule ^ - [F]
</Macro>

# vim: ft=apache ts=4 sts=4 sw=4 et
